# Copy to .env.local for the local compose stack (make compose-local-up).
# Host ports differ from deploy/.env.example so both stacks can run side by side.

DATABASE_URL=postgres://coppice:coppice@localhost:5433/coppice
COPPICE_SERVER_URL=http://localhost:8081
VITE_API_URL=http://localhost:8081
SESSION_SECRET=dev-secret-change-me
COPPICE_BOOTSTRAP_PASSWORD=changeme
